[project]
name = "game2048"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.40",
    "openai",
    "python-dotenv",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
